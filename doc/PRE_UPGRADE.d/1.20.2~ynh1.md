- new release [1.20.2](https://github.com/9001/copyparty/releases/tag/v1.20.2)
- support for sub-path install (the app no longer requires full domain, installs on `https://domain.tld/copyparty` should work)
- switched to username-password login (was password only previously)
- fixed clash with SSOWat (users can log in when logged in via YunoHost SSO at the same time)